{
  "name": "sass-basic-study",
  "version": "1.0.0",
  "description": "sass 공부할거야",
  "main": "index.js",
  "scripts": {
    "sass": "node-sass -w -r sass/ -o css/ --output-style expanded --source-map-embed",
    "sass-build": "node-sass -r sass -o css --output-style compressed"
  }
}
